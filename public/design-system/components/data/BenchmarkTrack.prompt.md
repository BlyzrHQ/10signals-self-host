One benchmark dimension: a position track (◆ you, │ median, ○ leader) with every value printed, plus status and action.

```jsx
<BenchmarkTrack label="Trust readiness" hint="Shipping, returns, contact" yours={60} median={82} leader={95} leaderName="hearthwick.co" status="22 points behind market median" action="Expose clearer public evidence for returns." />
```

Stack several inside a Card with a legend; uses logical insets so it mirrors in RTL.
