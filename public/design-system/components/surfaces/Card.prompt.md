Flat content card; no shadow. Kicker (uppercase) → title → body → meta.

```jsx
<Card kicker="Largest proven gap" title="Trust readiness" meta="22 points behind market median" />
```

`dashed` variant frames proposed modules; `raised` is for rows inside a surface card.
