Square 28px bordered icon button for row-level controls such as the "Why this match?" drawer toggle.

```jsx
<IconButton label="Why this match?" pressed={open}>▾</IconButton>
```

Content is a glyph or small SVG. `pressed` tints it blue.
