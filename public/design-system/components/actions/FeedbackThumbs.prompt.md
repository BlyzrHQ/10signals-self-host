Subtle thumbs up/down pair for rating a single comparison; grey until pressed, blue tint when selected, click again to clear.

```jsx
<FeedbackThumbs value={vote} onChange={setVote} />
```

Pass Arabic `labels` when the page is RTL. Sits at the end of a match row or card footer.
