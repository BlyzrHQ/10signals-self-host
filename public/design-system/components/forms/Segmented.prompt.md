Pill segmented control for 2–4 mutually exclusive views (By product / Table / Matchups / Opportunities; English / العربية).

```jsx
<Segmented value={v} onChange={setV} options={[{value:'byproduct',label:'By product'},{value:'table',label:'Table'}]} />
```

Selected segment is blue-tinted.
